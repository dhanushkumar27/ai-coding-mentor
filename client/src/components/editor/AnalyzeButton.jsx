const AnalyzeButton = ({ onClick }) => {
  return (
    <button
      className="btn btn-primary w-100"
      onClick={onClick}
    >
      Analyze
    </button>
  );
};

export default AnalyzeButton;